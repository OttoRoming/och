# name "kmod"
# version "34.2"
# description "libraries and utilities for loading kernel modules"
# url "https://github.com/kmod-project/kmod"
# source tar "https://www.kernel.org/pub/linux/utils/kernel/kmod/kmod-34.2.tar.xz"
# ---

cd kmod-34.2

mkdir -p build
cd       build

meson setup --prefix=/usr ..    \
            --buildtype=release \
            -D manpages=false

ninja
ninja install
