# name "zlib"
# version "1.3.1"
# description "Compression and decompression routines used by some programs"
# url "https://zlib.net/"
# source tar "https://zlib.net/fossils/zlib-1.3.1.tar.gz"
# ---

cd zlib-1.3.1

./configure --prefix=usr

make
make install

# Remove a useless static library:
rm -fv "$DESTDIR/usr/lib/libz.a"
